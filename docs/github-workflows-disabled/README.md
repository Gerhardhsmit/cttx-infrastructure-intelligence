# Disabled GitHub workflow files

These workflow files were preserved outside `.github/workflows` because the available GitHub token for this export cannot create or update active GitHub Actions workflow files. If you want to enable CI later, copy the desired files back into `.github/workflows` using a GitHub token/account with the required workflow permission.
